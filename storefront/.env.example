# URLs the success page links to after purchase. Defaults assume local dev
# (classroom app on :5173); set these per deployment.
VITE_APP_URL=http://localhost:5173
VITE_CMS_URL=http://localhost:5173/cms/login
